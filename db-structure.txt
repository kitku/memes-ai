Table memes (
	id int(11) primary key not null,
	image varchar(50),
	caption varchar(255)
)

Table labels (
	id int(11) not null,
	label1 bit() default b'0',
	label2 bit() default b'0',
	...
	labeln bit() default b'0''
);

#How it should be according to olus2000
Table memes (
  id int(11) primary key not null,
  image varchar(50),
  caption varchar(255),
  [labelName1] bit() default b'0',
  [labelName2] bit() default b'0',
  ...
  [labelNameN] bit() default b'0'
);

#Not implemented
Table traits (
	id int() not null,
	trait1 int(),
	trait2 int(),
	...
	traitn int()
);

Table listoflabels (
	labelname varchar()
);
#not implemented
Table listoftraits (
	traitname varchar()
);

Table featured (
	id int(11) not null,
	timesShown int(11) default 0
);

